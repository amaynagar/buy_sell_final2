# Docker Notes

This directory is reserved for Docker-related assets and future environment-specific overrides.

## Local development

Use the root `docker-compose.yml`:

```bash
docker compose up --build
```

The app is available at `http://localhost:3000`.
